<h1>Index View</h1>

<% posts.forEach(post => { %>
    <h3>Title : <%= post.title  %></h3>
    <div><%= post.description  %></div>
    <div><%= post.price %>$</div>
    <div><%= post.location  %></div>
    <a href="/posts/<%= post.id %>">view more</a>
<% }) %>